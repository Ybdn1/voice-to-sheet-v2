name: voice_to_sheet_frontend
description: "VoiceToSheet mobile app for wastewater field agents."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  archive: ^3.6.1
  connectivity_plus: ^6.1.4
  cupertino_icons: ^1.0.8
  excel: ^4.0.6
  http: ^1.5.0
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  share_plus: ^10.1.4
  shared_preferences: ^2.5.3
  speech_to_text: ^7.3.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
